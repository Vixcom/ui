{
  "name": "onedisk-dashoard",
  "scripts": {
    "serve:styles": "sass styles:css --no-source-map --watch",
    "build:styles": "sass styles:css --no-source-map",
    "deploy": "npm run build:styles && firebase deploy --only hosting:dashboard"
  }
}